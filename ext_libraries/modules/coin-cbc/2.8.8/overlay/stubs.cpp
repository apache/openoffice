#include "CbcModel.hpp"
#include <string>

// Stub: CbcClpUnitTest is defined in unitTestClp.cpp (excluded).
// CbcMain1 declares but only calls it in test/miplib mode; return -1 (skip).
int CbcClpUnitTest(const CbcModel &, const std::string &, int, const double *)
{
    return -1;
}
