# migration note
Ant is avoided and we build with bazel directly.
# check?
Need to verify the exact source path inside the tarball: servletapi/jsr154/src/share/javax/servlet/**. 
do we need to add this?
