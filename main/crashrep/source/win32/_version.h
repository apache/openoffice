/* soreport.cpp includes this header but uses none of its macros at compile
 * time — it reads version info from version.ini at runtime.  The original
 * dmake build generated this from minormkchanged.flg; an empty stub is
 * sufficient for the Bazel build.
 */
#ifndef _VERSION_H_
#define _VERSION_H_
#endif
