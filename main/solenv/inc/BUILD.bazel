# main/solenv/inc — shared RC-compiler header fragments and version data.

exports_files([
    "version.lst",   # source of version numbers; read by gen_versionlist_hrc.pl
    "version.hrc",   # includes versionlist.hrc + verinfo.hrc; used in soreport.rc
    "verinfo.hrc",   # version string macros; included by version.hrc
])
