load("//main/staging:collect_files_aspect.bzl", "collect_outputs", "flat_install", "res_stage", "tree_install")

# ── Profile INI files (equivalent to scp2-generated profiles in dmake) ────────
# All ini files land in program/ alongside the DLLs and EXEs.
filegroup(
    name = "profile_ini",
    srcs = [
        "bootstrap.ini",
        "fundamental.ini",
        "soffice.ini",
        "uno.ini",
        # PYUNO_LOADER_PYTHONHOME / PYTHONPATH for pythonloader.dll.
        "pythonloader.uno.ini",
        # Vendor settings + JRE-selection data paths for jvmfwk.dll.  Name fixed
        # by fwkutil.hxx, not by us — see the file's own header.
        "jvmfwk3.ini",
    ],
)

# ── Debug-mode application manifest ──────────────────────────────────────────
# When --compilation_mode=dbg, toolchain uses /MDd which requires MSVCR90D.dll.
# Without an EXE manifest, Windows can't activate the debug CRT SxS assembly
# and raises R6034.  This manifest declares Microsoft.VC90.DebugCRT as a
# private assembly dependency so Windows resolves it from program/ instead of
# WinSxS (which doesn't carry the debug CRT on non-developer machines).
# Must be paired with Microsoft.VC90.DebugCRT.manifest (from @msvc_debug_crt).
# It ALSO declares Microsoft.Windows.Common-Controls 6.0.0.0 — same as the release
# manifest below — to enable visual styles; without it OpenThemeData() returns NULL,
# IsNativeControlSupported() is FALSE for all controls, and VCL silently uses bitmap
# radio/checkbox indicators (debug-only divergence from release).
filegroup(
    name = "soffice_exe_manifest",
    srcs = [
        # EXE application manifest — declares Microsoft.VC90.DebugCRT +
        # Microsoft.Windows.Common-Controls (visual styles) dependencies.
        "soffice.exe.manifest",
        # Private SxS assembly manifest — tells Windows to resolve msvcr90d.dll
        # and msvcp90d.dll from program\ rather than WinSxS.
        "Microsoft.VC90.DebugCRT.manifest",
    ],
)

# Release application manifest for soffice.exe — declares the Microsoft.VC90.CRT
# dependency so Windows consults SxS/publisher-policy and loads MSVCR90.dll from
# WinSxS with an activation context (no R6034).  Wrapped in a filegroup because
# collect_outputs.deps does not accept raw source files.
filegroup(
    name = "soffice_app_manifest",
    srcs = select({
        # x64 needs an amd64 CRT/assembly manifest — the loader rejects an x86
        # processorArchitecture manifest against the amd64 msvcr90.dll (0xC000007B).
        "//build:arch_x64": ["//main/desktop:util/amd64/soffice.exe.manifest"],
        "//conditions:default": ["//main/desktop:util/soffice.exe.manifest"],
    }),
)

# ── Flat program/ files (DLLs, EXEs, RDBs, ZIPs, .py scripts, .ini) ─────────
collect_outputs(
    name = "all_files",
    deps = [
        # ── Bootstrap INI files (scp2-equivalent) ────────────────────────────
        ":profile_ini",

        # ── Core launcher (pulls compile-time transitive DLLs transitively) ──
        "//main/desktop:soffice",

        # ── Crash reporter ────────────────────────────────────────────────────
        "//main/crashrep:crashrep",

        # ── JVM framework (required by cui, svl, svtools, desktop at load time) ─
        "//main/jvmfwk:jvmfwk",
        # Vendor plugin — the half that actually knows how to find and start a
        # JRE.  jvmfwk osl_loadModule()s it by the path in javavendors.xml
        # ("vnd.sun.star.expand:$URE_INTERNAL_LIB_DIR/sunjavaplugin.dll"), so
        # nothing links it and it must be listed here explicitly.  Missing, the
        # office reports no Java installed however many JDKs are present.
        "//main/jvmfwk:sunjavaplugin",

        # ── UNO runtime infrastructure ────────────────────────────────────────
        "//main/stoc:bootstrap.uno",
        "//main/stoc:stocservices.uno",
        # UNO reflection/introspection/invocation components (needed by BASIC/scripting)
        "//main/stoc:reflection.uno",
        "//main/stoc:introspection.uno",
        "//main/stoc:invocation.uno",
        "//main/stoc:invocadapt.uno",
        "//main/stoc:namingservice.uno",
        "//main/stoc:proxyfac.uno",
        "//main/binaryurp:binaryurp",
        # Native JNI bridge (java_uno.dll). Loaded by jurt's NativeThreadPool via
        # System.loadLibrary("java_uno") once Java is enabled. The Java side
        # (java_uno.jar) needs the program/classes/ classpath staging, which does
        # not exist yet — see readme.md "jni_uno" notes.
        "//main/bridges:java_uno",
        # jpipe.dll + jpipx.dll — the JNI half of jurt's NAMED-PIPE connection
        # (com.sun.star.lib.connections.pipe.PipeConnection does
        # System.loadLibrary("jpipe"), and jpipe's DllMain then loads jpipx
        # beside it).  Nothing in the office links either, and the office never
        # loads them itself — it is the JAVA CLIENT PROCESS that does, which is
        # why they were missing without breaking anything visible.  Any external
        # Java program connecting to a running office over a pipe needs them
        # (upstream ships both in the URE lib dir); here it is the qa/complex
        # suites, whose org.openoffice.test.OfficeConnection resolves
        # "uno:pipe,name=…;urp;StarOffice.ComponentContext".
        "//main/jurt:jpipe",
        "//main/jurt:jpipx",
        # com.sun.star.loader.Java2, in two halves: javavm starts/locates the
        # JVM through jvmfwk, javaloader is the loader that builds a class
        # loader over a component's jar.  Registered in services.rdb by
        # //main/postprocess.  NOTE these being present is necessary but NOT
        # sufficient to run a Java component — program/classes/ staging and the
        # URE_INTERNAL_JAVA_DIR bootstrap variable are still missing; see
        # main/stoc/readme.md.
        "//main/stoc:javaloader.uno",
        "//main/stoc:javavm.uno",
        "//main/io:streams",
        "//main/io:acceptor",
        "//main/io:connector",
        "//main/io:textinstream",
        "//main/io:textoutstream",
        "//main/remotebridges:uuresolver",
        "//main/configmgr:configmgr.uno",

        # ── Storage / XML / UCB ───────────────────────────────────────────────
        "//main/package:package2",
        "//main/package:xstor",
        "//main/unoxml:unoxml",
        "//main/unoxml:unordf",
        "//main/xmlhelp:tvhlp1",
        "//main/xmlhelp:ucpchelp1",
        "//main/ucb:ucb1",
        "//main/ucb:cached1",
        "//main/ucb:srtrs1",
        "//main/ucb:ucpfile1",
        "//main/ucb:ucphier1",
        "//main/ucb:ucppkg1",
        "//main/ucb:ucpdav1",
        "//main/ucb:ucpftp1",
        "//main/ucb:ucpexpand1",
        "//main/ucb:ucpext",
        "//main/ucb:ucptdoc1",
        "//main/fileaccess:fileacc",
        "//main/shell:localebe1.uno",
        "//main/shell:wininetbe1.uno",
        "//main/shell:syssh.uno",

        # ── SAX ──────────────────────────────────────────────────────────────
        "//main/sax:expwrap",
        "//main/sax:fastsax",

        # ── i18n ──────────────────────────────────────────────────────────────
        "//main/i18npool/pool:i18npool",
        "//main/i18npool/pool:i18nsearch",
        # Per-locale data libraries — loaded by LocaleDataImpl::getLocaleItem()
        # via osl_loadModule(localedata_<group>).  Missing these makes every
        # locale lookup throw, so Writer/Calc text + caret init fails (the Start
        # Center, which lays out no editable text, still works).
        "//main/i18npool:localedata_en",
        "//main/i18npool:localedata_es",
        "//main/i18npool:localedata_euro",
        "//main/i18npool:localedata_others",

        # ── Linguistic ────────────────────────────────────────────────────────
        "//main/lingucomponent:spell",
        "//main/lingucomponent:hyphen_uno",
        "//main/lingucomponent:lnth",
        "//main/lingucomponent:guesslang",

        # ── Canvas backends ───────────────────────────────────────────────────
        "//main/canvas:canvasfactory",
        "//main/canvas:simplecanvas",
        "//main/canvas:vclcanvas",
        "//main/canvas:directx9canvas",
        "//main/canvas:gdipluscanvas",

        # ── Scripting & Python ────────────────────────────────────────────────
        "//main/scripting:scriptframe",
        "//main/scripting:basprov.uno",
        "//main/scripting:dlgprov.uno",
        "//main/scripting:vbaevents.uno",
        "//main/scripting:stringresource.uno",
        "//main/scripting:protocolhandler",
        # pyuno.pyd, NOT pyuno.dll: Python 2.7 imports only ".pyd".  Staging
        # both would load the bridge twice under two HMODULEs, each with its own
        # PyUNOType/RuntimeImpl_Type statics — see main/pyuno/util/pyuno.def.
        "//main/pyuno:pyuno_pyd",
        "//main/pyuno:pythonloader",
        # pythonloader.py / uno.py / unohelper.py — imported by the loader.
        "//main/pyuno:pyuno_py",
        # pythonscript.py — the ScriptProviderForPython implementation itself,
        # resolved by `import pythonscript` from the services.rdb pymodule URI.
        "//main/scripting:pyprov_py",

        # ── VBA ───────────────────────────────────────────────────────────────
        "//main/vbahelper:vbahelper",
        "//main/vbahelper:msforms",

        # ── File picker ───────────────────────────────────────────────────────
        "//main/fpicker:fps",
        "//main/fpicker:fop",
        "//main/fpicker:fps_office",
        "//main/fpicker:fpicker",

        # ── Chart ─────────────────────────────────────────────────────────────
        "//main/chart2:charttools",
        "//main/chart2:chartmodel",
        "//main/chart2:chartview",
        "//main/chart2:chartcontroller",

        # ── Animations / slideshow ────────────────────────────────────────────
        "//main/animations:animcore",
        "//main/slideshow:slideshow",

        # ── SVG I/O ───────────────────────────────────────────────────────────
        "//main/svgio:svgio",

        # ── Filters ───────────────────────────────────────────────────────────
        "//main/filter:msfilter",
        "//main/filter:xmlfa",
        "//main/filter:xfld",
        "//main/filter:svgfilter",
        "//main/filter:pdffilter",
        "//main/filter:flash",
        "//main/filter:placeware",
        "//main/filter:t602filter",
        "//main/filter:filterconfig1",
        "//main/filter:xsltfilter",
        "//main/filter:xsltdlg",
        # Graphic format import/export filters
        "//main/filter:egi",
        "//main/filter:eme",
        "//main/filter:epb",
        "//main/filter:epg",
        "//main/filter:epp",
        "//main/filter:era",
        "//main/filter:eti",
        "//main/filter:exp",
        "//main/filter:ept",
        "//main/filter:eps",
        "//main/filter:ime",
        "//main/filter:ipb",
        "//main/filter:icd",
        "//main/filter:ipx",
        "//main/filter:ipt",
        "//main/filter:idx",
        "//main/filter:iti",
        "//main/filter:ipd",
        "//main/filter:itg",
        "//main/filter:ira",
        "//main/filter:ips",
        "//main/filter:icg",

        # ── Embedded objects ──────────────────────────────────────────────────
        "//main/embeddedobj:embobj",
        "//main/embeddedobj:emboleobj",

        # ── Accessibility ─────────────────────────────────────────────────────
        "//main/accessibility:acc",

        # ── Data transfer ─────────────────────────────────────────────────────
        "//main/dtrans:dtrans",
        "//main/dtrans:mcnttype",
        "//main/dtrans:dnd",
        "//main/dtrans:ftransl",
        "//main/dtrans:sysdtrans",

        # ── MS Open XML ───────────────────────────────────────────────────────
        "//main/oox:oox",

        # ── Writer ────────────────────────────────────────────────────────────
        "//main/writerfilter:writerfilter",
        "//main/sw:swd",
        "//main/sw:sw",
        "//main/sw:swui",
        "//main/sw:msword",
        "//main/sw:vbaswobj",

        # ── Calc ──────────────────────────────────────────────────────────────
        "//main/sc:sc",
        "//main/sc:scd",
        "//main/sc:scfilt",
        "//main/sc:scui",
        "//main/sc:vbaobj",
        "//main/scaddins:analysis",
        "//main/scaddins:date",
        "//main/sccomp:solver",

        # ── Impress / Draw ────────────────────────────────────────────────────
        "//main/sd:sdd",
        "//main/sd:sd",
        "//main/sd:sdui",
        "//main/sd:sdfilt",
        "//main/sdext:PresentationMinimizer",
        "//main/sdext:PresenterScreen",

        # ── Math ──────────────────────────────────────────────────────────────
        "//main/starmath:sm",
        "//main/starmath:smd",

        # ── Base / Database ───────────────────────────────────────────────────
        "//main/connectivity:dbtools",
        "//main/connectivity:sdbc2",
        "//main/connectivity:dbpool2",
        # SDBC drivers.  file.dll is not a component and nothing registers it —
        # it is the shared implementation dbase/flat/calc link against — but it
        # must be staged all the same or those three fail to load.
        "//main/connectivity:file",
        "//main/connectivity:dbase",
        "//main/connectivity:flat",
        "//main/connectivity:calc",
        "//main/connectivity:jdbc",
        "//main/connectivity:hsqldb",
        "//main/dbaccess:dba",
        "//main/dbaccess:dbu",
        "//main/dbaccess:dbaxml",
        "//main/dbaccess:sdbt",
        "//main/dbaccess:dbmm",
        "//main/dbaccess:adabasui",
        "//main/reportdesign:rpt",
        "//main/reportdesign:rptui",
        "//main/reportdesign:rptxml",

        # ── Basic IDE / Common UI / Controls ──────────────────────────────────
        "//main/basctl:basctl",
        "//main/cui:cui",
        "//main/formula:for",
        "//main/formula:forui",
        "//main/UnoControls:ctl",
        "//main/eventattacher:evtatt",

        # ── User interaction handler ──────────────────────────────────────────
        # uui.dll: UUIInteractionHandler — instantiated as the medium's
        # interaction handler during every document load (passwords, filter
        # selection, name clashes, SSL/locking warnings).  Missing it makes
        # SfxMedium's load throw "loading component library failed .../uui.dll",
        # aborting any File>Open.
        "//main/uui:uui",

        # ── Framework extras (not compile-time dep of soffice) ────────────────
        "//main/framework:fwl",
        "//main/framework:fwm",

        # ── svl / svtools extras ──────────────────────────────────────────────
        "//main/svl:fsstorage",
        "//main/svl:passwordcontainer",
        "//main/svtools:hatchwindowfactory",

        # ── xmloff / svx extras ───────────────────────────────────────────────
        "//main/xmloff:xof",
        "//main/svx:textconversiondlgs",

        # ── XML security ──────────────────────────────────────────────────────
        "//main/xmlsecurity:xsec_fw",
        "//main/xmlsecurity:xsec_xmlsec",
        "//main/xmlsecurity:xmlsecurity",

        # ── Forms ─────────────────────────────────────────────────────────────
        "//main/forms:frm",

        # ── Extensions (UNO component plugins) ────────────────────────────────
        "//main/extensions:log",
        "//main/extensions:xmx",
        "//main/extensions:res",
        "//main/extensions:scn",
        "//main/extensions/source/oooimprovement:oooimprovement",
        "//main/extensions:oooimprovecore",
        "//main/extensions:updatefeed",
        "//main/extensions:bib",
        "//main/extensions:dbp",
        "//main/extensions:abp",
        "//main/extensions:preload",
        "//main/extensions:pcr",
        "//main/extensions:updchk",
        "//main/extensions:updchkui",

        # ── Postprocess outputs (RDB, ZIP) ────────────────────────────────────
        "//main/postprocess:all",
        "//main/offapi:types_rdb",
        "//main/oovbaapi:oovbaapi_rdb",

        # ── Python ────────────────────────────────────────────────────────────
        "@python//:python",
        "@python//:python27",
    ] + select({
        # ATL-gated modules (VS Professional/Enterprise only; not Express/BuildTools).
        # Enable with:  build --//build:atl_available=True  in user.bazelrc
        "//build:has_atl": [
            "//main/embedserv:emser",
            "//main/embedserv:inprocserv",
            "//main/winaccessibility:uacccom",
            "//main/winaccessibility:winaccessibility",
            "//main/extensions:oleautobridge.uno",
        ],
        "//conditions:default": [],
    }) + select({
        # Debug CRT DLLs (MSVCR90D.dll, MSVCP90D.dll) and private-assembly
        # manifest — required when the toolchain uses /MDd (--compilation_mode=dbg).
        # The DLLs are not in System32; the manifest suppresses R6034 by
        # activating Microsoft.VC90.DebugCRT as a private SxS assembly.
        # Set MSVC_DEBUG_CRT_PATH in user.bazelrc if VS2008 is not at the
        # default path (see //build:vs_paths.bzl).
        "//build:dbg_build": [
            "@msvc_debug_crt//:dlls",
            ":soffice_exe_manifest",
        ],
        # Release (/MD): do NOT bundle app-local CRT DLLs.  Instead stage the
        # application manifest so soffice.exe declares a Microsoft.VC90.CRT
        # 9.0.30729.1 dependency.  That manifest is what makes Windows consult
        # SxS: the installed VC90 publisher policy
        # (x86_policy.9.0.microsoft.vc90.crt_..._9.0.30729.9635) redirects
        # 9.0.30729.1 → 9.0.30729.9635, which is present in WinSxS, so MSVCR90.dll
        # loads WITH an activation context (no R6034).
        # Without the manifest the loader never consults SxS/policy and searches
        # MSVCR90.dll only on the plain DLL path (program\, System32, PATH) — it is
        # in none of those, so the process dies before any window ("no window").
        # The :soffice cc_binary links /MANIFEST:NO, so this external .manifest is
        # the only manifest soffice.exe gets.  (A redistributable build for
        # machines lacking the VC9 redist would additionally ship the CRT as a
        # program\Microsoft.VC90.CRT\ subfolder private assembly + hash-free
        # manifest; on a dev box with the redist installed WinSxS covers it.)
        "//conditions:default": [
            ":soffice_app_manifest",
        ],
    }) + select({
        # C++/UNO bridge, loaded by name from CPPU_ENV: msci_uno.dll on x86,
        # mscx_uno.dll on x64.  Each target is target_compatible_with its own
        # arch, so the unused one must not appear in the other arch's deps.
        "//build:arch_x64": ["//main/bridges:mscx_uno"],
        "//conditions:default": ["//main/bridges:msci_uno"],
    }),
)

flat_install(
    name = "_install_program",
    srcs = [":all_files"],
    install_pdb = select({
        # Stage .pdb debug-info files alongside DLLs/EXEs.
        # Also requires --compilation_mode=dbg (or --features=generate_pdb_file)
        # so the toolchain actually produces PDB files.
        # Enable with:  build --//build:debug_info=True --compilation_mode=dbg
        "//build:has_debug": True,
        "//conditions:default": False,
    }),
)

# ── share/registry — XCD configuration packages ───────────────────────────────
# configmgr.uno.dll reads all *.xcd files from ${OOO_BASE_DIR}/share/registry/
# on startup.  Without them the app exits immediately with no UI.
tree_install(
    name = "_install_registry",
    src  = "//main/postprocess:all_xcd",
    strip_prefix = "main/postprocess",
    dst  = "share/registry",
)

# ── share/setup — scp2 installer script ──────────────────────────────────────
# setup_osl.inf is consumed by instsetoo_native (still deferred) to produce the
# Windows MSI installer.  Staged here so the artifact is reachable alongside the
# rest of the install layout.
tree_install(
    name = "_install_scp2",
    src  = "//main/scp2:setup_osl",
    strip_prefix = "main/scp2/setup",
    dst  = "share/setup",
)

# ── program/resource/ — OOo binary resource (.res) files ────────────────────
# ResMgrContainer::init() scans $OOO_BASE_DIR/program/resource/ on startup.
# Files must be named <prefix><lang>-<country>.res (e.g. vclen-US.res).
# Prefix comes from CreateResMgr("vcl",...) calls in each module.
res_stage(
    name = "_install_resources",
    res_files = {
        "//main/accessibility:accessibility_res":      "accen-US",
        "//main/dbaccess:adabasui_res":                "adabasuien-US",
        "//main/scaddins:analysis_res":                "analysisen-US",
        "//main/avmedia:avmedia_res":                  "avmediaen-US",
        "//main/basctl:basctl_res":                    "basctlen-US",
        "//main/basic:basic_res":                      "sben-US",
        "//main/chart2:chart2_res":                    "chartcontrolleren-US",
        # Three SEPARATE bundles, looked up by name at runtime, not one merged
        # .res: cnr is the shared driver strings (dbtools is compiled with
        # CONN_SHARED_RESOURCE_FILE=cnr), sdbcl is what JDriver.cxx's
        # ResourceBasedEventLogger("sdbcl", "org.openoffice.sdbc.jdbcBridge")
        # resolves, and sdberr holds the SQL error messages.  connectivity's
        # fourth reslib, hsqldb, is deliberately absent — it exists only to pull
        # its two images into images.zip and upstream never installs it.
        "//main/connectivity:connectivity_res":        "cnren-US",
        "//main/connectivity:connectivity_log_res":    "sdbclen-US",
        "//main/connectivity:connectivity_err_res":    "sdberren-US",
        "//main/cui:cui_res":                          "cuien-US",
        "//main/scaddins:date_res":                    "dateen-US",
        "//main/dbaccess:dba_res":                     "dbaen-US",
        "//main/dbaccess:dbmm_res":                    "dbmmen-US",
        "//main/dbaccess:dbui_res":                    "dbuien-US",
        "//main/desktop:desktop_res":                  "dkten-US",
        "//main/desktop:deployment_res":               "deploymenten-US",
        "//main/desktop:deploymentgui_res":            "deploymentguien-US",
        "//main/editeng:editeng_res":                  "editengen-US",
        "//main/extensions:bib_res":                   "biben-US",
        "//main/extensions:dbp_res":                   "dbpen-US",
        "//main/extensions:abp_res":                   "abpen-US",
        "//main/extensions:preload_res":               "preloaden-US",
        "//main/extensions:pcr_res":                   "pcren-US",
        "//main/extensions:upd_res":                   "upden-US",
        "//main/extensions:updchkui_res":              "updchken-US",
        "//main/filter:flash_res":                     "flashen-US",
        "//main/forms:forms_res":                      "frmen-US",
        "//main/formula:formula_res":                  "foren-US",
        "//main/fpicker:fpicker_res":                  "fps_officeen-US",
        "//main/framework:framework_res":              "fween-US",
        "//main/svx:gal_res":                          "galen-US",
        "//main/filter:graphicfilter_eps_res":         "epsen-US",
        "//main/sdext:minimizer_res":                  "minimizeren-US",
        "//main/svx:ofa_res":                          "ofaen-US",
        "//main/filter:pdf_res":                       "pdffiltreen-US",
        "//main/sdext:presenter_res":                  "PresenterScreenen-US",
        "//main/reportdesign:rpt_res":                 "rpten-US",
        "//main/reportdesign:rptui_res":               "rptuien-US",
        "//main/sc:sc_res":                            "scen-US",
        "//main/sccomp:sccomp_res":                    "solveren-US",
        "//main/scripting:scripting_res":              "scriptframeen-US",
        "//main/sd:sd_res":                            "sden-US",
        "//main/dbaccess:sdbt_res":                    "sdbten-US",
        "//main/sfx2:sfx2_res":                        "sfxen-US",
        "//main/starmath:starmath_res":                "smen-US",
        "//main/svl:svl_res":                          "svlen-US",
        "//main/svtools:svtools_res":                  "svten-US",
        "//main/svx:svx_res":                          "svxen-US",
        "//main/sw:sw_res":                            "swen-US",
        "//main/filter:t602_res":                      "t602filter680en-US",
        "//main/svx:textconversiondlgs_res":           "textconversiondlgsen-US",
        "//main/uui:uui_res":                          "uuien-US",
        "//main/vcl:vcl_res":                          "vclen-US",
        "//main/xmlsecurity:xmlsecurity_res":          "xmlsecen-US",
        "//main/filter:xsltdialog_res":                "xsltdlgen-US",
    },
)

# ── extras: share/ data directories ──────────────────────────────────────────
# Shared autotext (.dat, .bau), wordbook dictionaries, gallery SVG/GIF objects,
# document templates, bibliography database, and the OpenSymbol font.

tree_install(
    name = "_install_autotext",
    src          = "//main/extras:autotext_files",
    strip_prefix = "main/extras/source/autotext",
    dst          = "share/autotext",
)

# share/Scripts/python — the "OpenOffice Macros" Python container.
tree_install(
    name = "_install_python_scripts",
    src          = "//main/scripting:python_example_scripts",
    strip_prefix = "main/scripting/examples/python",
    dst          = "share/Scripts/python",
)

tree_install(
    name = "_install_wordbook",
    src          = "//main/extras:wordbook_files",
    strip_prefix = "main/extras/source/wordbook",
    dst          = "share/wordbook",
)

tree_install(
    name = "_install_gallery",
    src          = "//main/extras:gallery_files",
    strip_prefix = "main/extras/source/gallery",
    dst          = "share/gallery",
)

tree_install(
    name = "_install_templates",
    src          = "//main/extras:template_files",
    strip_prefix = "main/extras/source/templates",
    dst          = "share/template",
)

tree_install(
    name = "_install_misc_config",
    src          = "//main/extras:misc_config_files",
    strip_prefix = "main/extras/source/misc_config",
    dst          = "share/config",
)

tree_install(
    name = "_install_database",
    src          = "//main/extras:database_files",
    strip_prefix = "main/extras/source/database",
    dst          = "share/database",
)

tree_install(
    name = "_install_opensymbol",
    src          = "//main/extras:opensymbol_ttf",
    strip_prefix = "main/extras/source/truetype/symbol",
    dst          = "share/fonts/truetype",
)

# ── extras: presets/ — initial user profile template ─────────────────────────
# UserInstall::finalize() copies $OOO_BASE_DIR/presets/ → $UserInstallation/user/
# on first launch.  Staging autotext + palettes into presets/ subdirectories
# ensures the directory exists and the user gets a useful initial profile.

tree_install(
    name = "_install_presets_autotext",
    src          = "//main/extras:autotext_files",
    strip_prefix = "main/extras/source/autotext",
    dst          = "presets/autotext",
)

tree_install(
    name = "_install_presets_config",
    src          = "//main/extras:palette_files",
    strip_prefix = "main/extras/source/palettes",
    dst          = "presets/config",
)

# Initial user BASIC library template (Standard-only script.xlc/dialog.xlc).
# Copied to $UserInstallation/user/basic/ by UserInstall::finalize() on first
# launch, so the application BASIC library container finds a valid (empty)
# user/basic/script.xlc instead of raising ERRCODE_IO_GENERAL.  Replaces the
# scp2 basicusr.zip seed (built by the deferred wizards module).
tree_install(
    name = "_install_presets_basic",
    src          = "//main/wizards:basic_user_config",
    strip_prefix = "main/wizards/source/config",
    dst          = "presets/basic",
)

# The empty "Standard" user library that the container index references.
# Without Standard/dialog.xlb the app BasicManager raises ERRCODE_IO_GENERAL
# ("Error loading BASIC of document .../user/basic/Standard/dialog.xlb/").
tree_install(
    name = "_install_presets_basic_standard",
    src          = "//main/wizards:basic_user_standard",
    strip_prefix = "main/wizards/source/standard",
    dst          = "presets/basic/Standard",
)

# ── images.zip — toolbar and UI icons ─────────────────────────────────────────
# share/config/images.zip    — default icon set (all modules)
# share/config/images_industrial.zip — industrial/tango theme overrides
# The classic set (images_classic.zip) requires extracting classic_images.tar.gz;
# deferred until the packimages pipeline is complete.

tree_install(
    name = "_install_images_zip",
    src          = "//main/default_images:images",
    strip_prefix = "main/default_images",
    dst          = "share/config",
)

tree_install(
    name = "_install_images_industrial_zip",
    src          = "//main/ooo_custom_images:images_industrial",
    strip_prefix = "main/ooo_custom_images",
    dst          = "share/config",
)

# ── program/classes/ — Java UNO runtime jars ($URE_INTERNAL_JAVA_DIR) ────────
# com.sun.star.loader.Java2 bootstraps from here.  javavm.cxx builds a
# URLClassLoader over "$URE_INTERNAL_JAVA_DIR/unoloader.jar" to reach
# com.sun.star.lib.unoloader.UnoClassLoader, whose createUrls() then hardcodes
# java_uno.jar / juh.jar / jurt.jar / ridl.jar relative to the same base.  All
# five are exact-string lookups, which is why the uno_jar rule fixes each output
# filename at the producing target rather than renaming during staging.
#
# unoil.jar is the exception: it is NOT one of the hardcoded names and arrives
# through the second channel — $URE_INTERNAL_JAVA_CLASSPATH → URE_MORE_JAVA_TYPES
# in program/fundamental.ini, passed to UnoClassLoader as its classPath argument.
#
# flatten: the six jars come from five different packages, so no single
# strip_prefix applies.
filegroup(
    name = "java_runtime_jars",
    srcs = [
        "//main/ridljar:unoloader_jar",
        "//main/ridljar:ridl_jar",
        "//main/jurt:jurt_jar",
        "//main/javaunohelper:juh_runtime_jar",
        "//main/bridges:java_uno_runtime_jar",
        "//main/unoil:unoil_jar",
        # The embedded-database pair.  Not part of the UNO runtime like the six
        # above, but they belong in the same directory: HDriver.cxx builds the
        # JDBC classpath as the literal
        # vnd.sun.star.expand:$OOO_BASE_DIR/program/classes/{hsqldb,sdbc_hsqldb}.jar
        # so program/classes is not a convention here, it is the lookup path.
        "//main/connectivity:hsqldb_jar",
        "//main/connectivity:sdbc_hsqldb_jar",
    ],
)

tree_install(
    name    = "_install_classes",
    src     = ":java_runtime_jars",
    flatten = True,
    dst     = "program/classes",
)

# ── program/ — jvmfwk's non-DLL runtime files ────────────────────────────────
# javavendors.xml (JRE-selection policy), JREProperties.class (the probe
# sunjavaplugin RUNS on a candidate JRE to read its properties) and
# sunjavaplugin.ini.  Not staged by _install_program: flat_install filters on
# _INSTALL_EXTS, and adding "xml"/"class" there would sweep in every such file
# in the transitive closure.  JREProperties.class in particular must land beside
# sunjavaplugin.dll — that directory is the -classpath the plugin passes.
tree_install(
    name    = "_install_java_config",
    src     = "//main/jvmfwk:runtime_config",
    flatten = True,
    dst     = "program",
)

# ── share/config/soffice.cfg — per-module UI configuration tree ───────────────
# The framework PresetHandler opens share/config/soffice.cfg as a *folder* and
# reads modules/<ModuleShortName>/{menubar,toolbar,statusbar}/*.xml at startup
# (the Start Center loads modules/startmodule first).  Without this tree the
# UIConfigurationManager throws CorruptedUIConfigurationException, surfacing as
# the "error loading user interface configuration data" FatalError.  The
# uiconfig_tree rule already roots its outputs at "soffice.cfg/...", so we strip
# only the package prefix and install into share/config.
tree_install(
    name = "_install_uiconfig",
    src          = "//main/postprocess:uiconfig_tree",
    strip_prefix = "main/postprocess",
    dst          = "share/config",
)

# Top-level install: program/ files + share/registry/ XCD packages + scp2 metadata.
filegroup(
    name = "install",
    srcs = [
        ":_install_program",
        ":_install_registry",
        ":_install_scp2",
        ":_install_resources",
        ":_install_autotext",
        ":_install_wordbook",
        ":_install_gallery",
        ":_install_templates",
        ":_install_misc_config",
        ":_install_database",
        ":_install_opensymbol",
        ":_install_presets_autotext",
        ":_install_presets_config",
        ":_install_presets_basic",
        ":_install_presets_basic_standard",
        ":_install_images_zip",
        ":_install_images_industrial_zip",
        ":_install_uiconfig",
        ":_install_classes",
        ":_install_java_config",
        ":_install_python_scripts",
        # The Python stdlib tree is NOT optional: pythonloader.dll calls
        # Py_Initialize(), and Python 2.7 exit()s the process if it cannot find
        # its stdlib.  Anything that instantiates com.sun.star.loader.Python
        # (Tools > Macros, script-provider enumeration) would kill soffice.
        ":install_python_lib",
        ":install_python_bin",
    ],
    visibility = ["//visibility:public"],
)

# ── Python standard library tree → program/python-core-2.7.18/lib/ ───────────
# Strip the "Lib" prefix so "Lib/abc.py" → "python-core-2.7.18/lib/abc.py".
tree_install(
    name = "install_python_lib",
    src          = "@python//:python_lib",
    strip_prefix = "Lib",
    dst          = "program/python-core-2.7.18/lib",
)

# ── python.exe inside the core tree → python-core-2.7.18/bin/python.exe ──────
# Same binary as in program/ root; installed again under the versioned tree.
tree_install(
    name = "install_python_bin",
    src          = "@python//:python",
    strip_prefix = "",
    dst          = "program/python-core-2.7.18/bin",
)

# ── install_all: kept as an alias for :install ────────────────────────────────
# The python-core tree used to live only here; it is now part of :install itself
# (see above), so the two are identical.  Retained so existing invocations and
# docs referring to //main/staging:install_all keep working.
filegroup(
    name = "install_all",
    srcs = [":install"],
)
